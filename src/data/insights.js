// Planned: Background flow detection, usage spike alerts
// Will be populated in a future update
// Insights will appear as: a tab on SystemDetail + a KPI card on Dashboard
export const INSIGHTS = [];
export function getInsightsForSystem(systemId) { return []; }
export function getInsightsCount() { return 0; }
